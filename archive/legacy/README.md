# Legacy Archive

这里保存当前 v7 主线不再直接使用的旧文件。

归档原则：

- 旧版 v4/v5/v6/v6.3/v6.5 启动脚本和备份。
- 旧 Flask 仪表盘。
- 旧回测、参数优化、版本对比脚本。
- 旧打包脚本和旧路径批处理。

这些文件暂时不删除，方便后续查历史逻辑、对比回测或回滚。

当前实盘入口在项目根目录：

```powershell
python run_web.py
```
